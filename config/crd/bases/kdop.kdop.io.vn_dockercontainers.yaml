---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: dockercontainers.kdop.kdop.io.vn
spec:
  group: kdop.kdop.io.vn
  names:
    kind: DockerContainer
    listKind: DockerContainerList
    plural: dockercontainers
    singular: dockercontainer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DockerContainer
            properties:
              containerName:
                description: ContainerName on the Docker host. Defaults to metadata.name
                  if empty.
                type: string
              dockerHostRef:
                description: |-
                  DockerHostRef names a DockerHost in the same namespace.
                  Empty = local unix socket (/var/run/docker.sock)
                type: string
              image:
                description: Image to run (required)
                minLength: 1
                type: string
              restartPolicy:
                default: always
                description: 'RestartPolicy: no | on-failure | always | unless-stopped'
                enum:
                - "no"
                - on-failure
                - always
                - unless-stopped
                type: string
            required:
            - image
            type: object
          status:
            description: status defines the observed state of DockerContainer
            properties:
              id:
                description: ID is the Docker container ID
                type: string
              ipv4:
                description: IPv4 on the Docker network (optional, phase sau tunnel)
                type: string
              state:
                description: 'State: running | exited | created | ...'
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

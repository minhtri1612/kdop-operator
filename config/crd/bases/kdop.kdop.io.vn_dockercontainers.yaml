---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: dockercontainers.kdop.kdop.io.vn
spec:
  group: kdop.kdop.io.vn
  names:
    kind: DockerContainer
    listKind: DockerContainerList
    plural: dockercontainers
    singular: dockercontainer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DockerContainer
            properties:
              containerName:
                description: ContainerName on the Docker host. Defaults to metadata.name
                  if empty.
                type: string
              dockerHostRef:
                description: |-
                  DockerHostRef names a DockerHost in the same namespace.
                  Empty = local unix socket (/var/run/docker.sock)
                type: string
              env:
                description: Env is a list of literal "KEY=VALUE" entries.
                items:
                  type: string
                type: array
              envVars:
                description: EnvVars supports literal Value or ValueFrom.SecretKeyRef.
                items:
                  description: EnvVar defines an environment variable.
                  properties:
                    name:
                      description: Name of the environment variable
                      type: string
                    value:
                      description: Value is a literal value (optional if ValueFrom
                        is set)
                      type: string
                    valueFrom:
                      description: ValueFrom reads the value from a K8s Secret
                      properties:
                        secretKeyRef:
                          description: SecretKeyRef selects a key of a Secret in the
                            same namespace
                          properties:
                            key:
                              description: Key of the secret to select
                              type: string
                            name:
                              description: Name of the Secret
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                type: array
              healthCheck:
                description: HealthCheck configures Docker's native health check.
                properties:
                  interval:
                    description: Interval between checks (e.g. "5s")
                    type: string
                  retries:
                    description: Retries before marking unhealthy
                    type: integer
                  test:
                    description: Test is the check command, e.g. ["CMD-SHELL", "pidof
                      nginx || exit 1"]
                    items:
                      type: string
                    type: array
                  timeout:
                    description: Timeout for one check (e.g. "3s")
                    type: string
                required:
                - test
                type: object
              image:
                description: Image to run (required)
                minLength: 1
                type: string
              imagePullSecret:
                description: |-
                  ImagePullSecret is a Secret with keys username, password, server
                  used to authenticate private registry pulls.
                type: string
              ports:
                items:
                  type: string
                type: array
              restartPolicy:
                default: always
                description: 'RestartPolicy: no | on-failure | always | unless-stopped'
                enum:
                - "no"
                - on-failure
                - always
                - unless-stopped
                type: string
              secretVolumes:
                description: SecretVolumes uploads K8s Secret keys as files into the
                  container.
                items:
                  description: SecretVolume maps a K8s Secret into a directory inside
                    the container.
                  properties:
                    mountPath:
                      description: MountPath is the absolute path in the container
                        (e.g. /etc/secrets)
                      type: string
                    secretName:
                      description: SecretName is the Secret in the same namespace
                      type: string
                  required:
                  - mountPath
                  - secretName
                  type: object
                type: array
              volumeMounts:
                description: VolumeMounts binds host paths into the container.
                items:
                  description: VolumeMount binds a host path into the container.
                  properties:
                    containerPath:
                      description: ContainerPath is the absolute path in the container
                      type: string
                    hostPath:
                      description: HostPath is the absolute path on the Docker host
                      type: string
                    readOnly:
                      description: ReadOnly mounts the volume read-only
                      type: boolean
                  required:
                  - containerPath
                  - hostPath
                  type: object
                type: array
            required:
            - image
            type: object
          status:
            description: status defines the observed state of DockerContainer
            properties:
              health:
                description: 'Health: healthy | unhealthy | starting | none'
                type: string
              id:
                description: ID is the Docker container ID
                type: string
              ipv4:
                description: IPv4 on the Docker network (optional, phase sau tunnel)
                type: string
              state:
                description: 'State: running | exited | created | ...'
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

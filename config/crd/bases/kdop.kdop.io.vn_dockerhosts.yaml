---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: dockerhosts.kdop.kdop.io.vn
spec:
  group: kdop.kdop.io.vn
  names:
    kind: DockerHost
    listKind: DockerHostList
    plural: dockerhosts
    singular: dockerhost
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.hostURL
      name: Host
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DockerHost
            properties:
              hostURL:
                description: |-
                  HostURL is the Docker daemon endpoint.
                  Examples: unix:///var/run/docker.sock, tcp://host:2376
                minLength: 1
                type: string
              tlsSecretName:
                description: TLSSecretName is a Secret (same namespace) with ca.pem,
                  cert.pem, key.pem
                type: string
            required:
            - hostURL
            type: object
          status:
            description: status defines the observed state of DockerHost
            properties:
              message:
                description: Message describes connection result or last error
                type: string
              phase:
                description: 'Phase: Connected | Error'
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
